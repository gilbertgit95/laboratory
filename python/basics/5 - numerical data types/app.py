pi = 2.1415926
diameter = input('Input circle diameter(number): ')
area = 0

# compute circle area
# formula [A = Pi * diameter] or [2 * Pi * r]
area = pi * int(diameter)
print(f'area is: { area }')

area = pi * float(diameter)
print(f'area is: { area }')